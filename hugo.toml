baseURL = 'https://kaustabpal.github.io/'
languageCode = 'en-us'
title = 'KP'

[params]
  description = "Engineer working at the intersection of deep learning and autonomous driving."

[markup]
  [markup.tableOfContents]
    startLevel = 2
    endLevel = 4
    ordered = false
  [markup.highlight]
    noClasses = false
    lineNos = true
    lineNumbersInTable = false
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
